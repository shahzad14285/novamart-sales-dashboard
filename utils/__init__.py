"""Utility package for the NovaMart Sales Intelligence Dashboard.

Contains framework-agnostic business logic (calculations, formatting,
generic helpers) and data-access functions. UI code (Streamlit) lives
in ``components/`` and ``pages/`` and should call into this package
rather than the other way around.
"""
