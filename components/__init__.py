"""Reusable Streamlit UI components for the NovaMart Dashboard.

Components in this package render UI only. They read data/formatted
values that are handed to them (or pulled via ``utils``) but never
contain business calculations themselves -- that logic belongs in
``utils/calculations.py``.
"""
